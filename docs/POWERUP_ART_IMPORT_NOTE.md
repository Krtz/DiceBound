# Powerup Art Import Note

This is a one-shot import record for the generated powerup art batch. The temporary importer/workflow removes itself after successful integration on `main`; this note remains so the source history explains how the binary assets and runtime mappings were introduced.
