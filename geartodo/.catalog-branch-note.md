# Catalogue branch coordination note

`catalog/runescape-armour-expansion` is intentionally a planning/catalogue-only branch while active implementation work proceeds elsewhere.

It must be rebased onto current `main`, assigned the next unused four-component DiceBound version per #64, and validated before any PR/merge. Do not merge it underneath an active Codex implementation branch merely to land catalogue text.
