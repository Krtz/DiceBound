## HUD overlap hotfix
- Fixed the Legacy Companion card being able to overlap the combined Character Stats/Gear card in short landscape windows.
- Removed retired responsive placement rules for the old standalone Equipment card.
- Retired the stale `data-sidebar-companion` layout state that could override the measured HUD flow.
- Two-column short landscape now stacks Character → Companion → Adventure Log safely.
- Three-column short landscape gives Character two columns, Companion one column, and keeps the Log below.
- Added a real Edge regression that calculates Character/Companion rectangle intersection at short-window sizes and during live resize.
