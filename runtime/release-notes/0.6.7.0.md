## Monolith Chainsaw

- Reduced `runtime/js/dicebound.js` from **7,177 lines / 649,410 bytes** at the start of the 0.6.7.0 cleanup to **4,884 lines / 386,185 bytes** at the stabilized Wave 10 checkpoint while preserving released gameplay, balance, RNG/event ordering, save/checkpoint semantics and intended UI behavior.
- Removed provably dead DB317 registry writes, redundant monolith-to-owner delegates, predecessor wrapper ladders and obsolete compatibility sediment; live responsibilities now route through their canonical subsystem owners instead of accumulating another patch layer in the composition monolith.
- Runtime architecture now contains **92 modules: 91 extracted/focused modules plus one Composition / Bootstrap / Tooling monolith**, with **zero repeated named functions** and **zero duplicate top-level function declarations**.
- Permanent behavior oracles, architecture/assets/version validators, historical-layer census and shadow-ownership anti-return coverage remain in the release gate. Temporary 0.6.7.0 mutation, materialization, bisect, diagnostic and surgery workflows/tools were removed before merge preparation.
- The remaining historical-layer score is **653** and is intentionally not a release blocker. Further legacy cleanup is now bug/feature-driven: reproduce the affected behavior, move any participating responsibility out of `dicebound.js`, normalize the canonical owner path, and fix the behavior there rather than adding another monolith wrapper.

### Stabilization

- Canonicalized Artifact and Class chooser ownership guards so the permanent tests follow the final direct owner-routing architecture instead of requiring adapters deliberately removed by the chainsaw.
- No gameplay feature or balance changes are intentionally included in this stabilization pass.
