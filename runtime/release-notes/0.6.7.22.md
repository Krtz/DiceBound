## Input & dismissal cleanup

- Finish #90 with one semantic app-shell input router: Space/Enter delegate to canonical Road Dice only when no modal owns input, text-editing controls are ignored, held/repeated keys cannot double-trigger, and Escape dismisses the top visible semantic surface or opens Options only when no gameplay overlay blocks it.
- Finish the #198 live dismissal audit for current UI generations. Options, Class, Pet, Talents, Prestige, Info and Achievements share one dismissal contract; legacy Merchant, Run Buffs and Debug surfaces now keep an always-visible top-right dismissal control.
- Fix #202 so Sealed Relic uses the canonical Powerup rarity progression with an **Uncommon+** floor instead of its old Rare/Epic-only pool.
- Remove the raw global Road Dice key listener from `dicebound.js`; the composition root now only injects Road Dice and Options actions into the focused input owner.
- No save-schema change and no intentional gameplay RNG-order change outside Sealed Relic's approved eligible rarity pool.
