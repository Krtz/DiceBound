## Camp / Gear / Mana / Class Chooser Cleanup

### Character Gear
- Move Ring out of the Chest footprint in the spatial paper doll.
- Move Amulet outward symmetrically so the torso remains visually clear.
- Preserve image-only occupied slots, rarity borders and authoritative hover/detail stats.

### Camp presentation
- Class, Nightmare, Heirloom Chest and Pet are now **art-only** controls in Camp.
- Their semantic buttons retain aria-label/title accessibility while the visible labels/subtitles are removed.
- Painted-object hit targeting keeps the click/focus area aligned with the authored artwork.
- Class chooser now owns one persistent Done action instead of duplicate outer/inner Camp controls.

### Pale Devil secret
- Retire the tiny-devil / circle-the-fire ritual implementation and hotspot.
- While Hell Mode is active, clicking the **bonfire itself** primes the existing Pale Devil encounter.
- Outside Hell Mode the bonfire is inert.
- Existing Board 2 Pale Devil generation, rewards and secret-boss semantics are unchanged.

### #411 — Mana builders
- Resolve Mana-builder gain immutably per action from canonical base gain + generic Mana-builder bonus + class-specific Summoner bonus.
- Never mutate shared `SPELLS[*].gain` values across awaited actions.
- Block re-entrant generator attempts so rapid double-input cannot double-grant Mana.
- Apply the same transaction discipline to Invoker Wex.
- Keep legitimate Quick Channel stacking and Slime Rouge borrowed-identity behavior.
- Report actual generated Mana in combat history, including max-Mana caps.

### #236 — Class chooser verification
- Real Edge coverage verifies wide, compact and short/wide centering.
- Live resize while open keeps the chooser centered and usable.
- One persistent Done remains reachable.
- Escape dismisses through the visible Done/shared dismissal path.
- Native WebView2 validation remains required before closing the issue.

### Architecture / cleanup
- Remove the retired Hell ritual state/listeners/hotspot code instead of leaving dormant branches.
- Keep Mana-builder resolution in the combat Mana owner and share it with Invoker.
- No unrelated RNG, save-schema, Board-generation, checkpoint or balance changes.
