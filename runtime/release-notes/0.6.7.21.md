## Career visibility hotfix

- Fix Career tabs whose content was rendered into the DOM but remained invisible because the inactive panel CSS selector had higher specificity than the active-panel selector.
- The active Career panel now explicitly wins with `#careerOverlay .career-panel.active { display:block }`.
- Strengthen the real Edge regression to verify computed visibility and non-zero geometry for Overview, Enemy Ledger and Run History, while ensuring inactive tabs remain hidden.
- No Career data model, telemetry, save-schema or migration behavior changes.
