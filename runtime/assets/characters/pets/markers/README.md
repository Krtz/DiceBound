# Pet markers

Reserved for dedicated companion board/token markers. The current runtime reuses pet portrait artwork where a pet image is needed; add `<pet-id>.png` here when dedicated markers become a real rendering context.
