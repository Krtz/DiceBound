# Hell mode toggle art

`on.png` is the approved active Hell-mode scene: a fire-sprouting volcano with a dancing devil. The Camp presentation owner renders it only while Hell is enabled; inactive/locked Hell remains a semantic Camp control rather than reusing this active artwork.

Keep future state-specific art here with clear semantic filenames such as `locked.png`, `available.png`, `off.png` and `on.png`.
