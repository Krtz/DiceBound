# Miniboss board markers

Reserved for independently replaceable miniboss board-tile artwork. For now the runtime explicitly reuses each miniboss battle image for its guardian tile marker; `runtime/js/assets.js` records that reuse instead of pretending a dedicated marker exists.
