# Normal enemy board markers

Canonical static board-tile/token art for ordinary enemies. Every normal enemy has one semantic `<enemy-id>.png` marker. A marker communicates identity only: it does not vary by Board or Normal/Nightmare/Hell mode, and it must never be used as full-body battle art.
