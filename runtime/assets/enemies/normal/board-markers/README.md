# Normal enemy board markers

Reserved for dedicated board-tile/token artwork for normal enemies. The current board uses generic/enemy presentation rather than separate per-enemy marker art. Use `<enemy-id>.png` when this context gains dedicated artwork.
