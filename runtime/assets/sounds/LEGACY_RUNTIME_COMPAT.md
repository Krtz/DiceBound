# Recovered Beta 0.6 sound path compatibility

The current semantic audio home is `assets/audio/`. This `assets/sounds/` tree is retained so historical runtime instructions/fallback paths still resolve. New packaged/override audio should follow `assets/audio/README.md`.
