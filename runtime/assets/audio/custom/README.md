# Optional custom SFX overrides

Drop files named `roll`, `step`, `hit`, `crit`, `coin`, `heal`, `lose`, `level`, `win`, or `holy` here as `.ogg`, `.mp3`, `.wav`, or `.webm`. Missing overrides fall back to the built-in synth.
