# Recovered Beta 0.6 UI path compatibility

`backgrounds/`, `class-art/`, `class-markers/`, `icon/` and `icons/` are retained as read-only compatibility mirrors because the recovered monolithic runtime still contains fallback literals pointing at those historical paths. **Do not add or replace current art here.** New/current art belongs in the semantic folders documented by `assets/README.md`.
