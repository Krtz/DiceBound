# Recovered Beta 0.6 pet path compatibility

`portraits/` mirrors the canonical `assets/characters/pets/portraits/` files because the recovered monolith still contains fallback literals under `assets/pets/portraits/`. New pet artwork belongs under `assets/characters/pets/`.
