# Dicebound Windows build resources — Beta 0.4

The old Edge `--app` launcher is retired. The primary Windows executable is now built from `wrappers/webview2/native-go/`.

This directory only retains the PE icon/version-resource helper used after the native wrapper is compiled.
